# Normalize line endings for all text files.
# Text files are stored and checked out with LF, matching the output of the
# toolchain (Angular schematics, ts-morph, formatters). Enforcing LF in the
# working tree avoids core.autocrlf/safecrlf conflicts on Windows.
* text=auto eol=lf
