import { TicksFormatterPipe } from './ticks-formatter.pipe';

describe('TicksFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TicksFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
