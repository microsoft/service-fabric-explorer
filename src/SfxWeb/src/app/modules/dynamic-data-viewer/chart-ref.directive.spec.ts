import { ChartRefDirective } from './chart-ref.directive';

describe('ChartRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartRefDirective();
    expect(directive).toBeTruthy();
  });
});
