percentage as byte: unresolved
object as param value: not have these for now
get app location: does it in both times
getdeployedreplica: adhoc vs app name: unresolved - use appname
add tests:
    how thorough?
        test each command?
        test all inputs?
        test styling?
    waiting until commands loaded how?
    test input how?
    test copy how?


restart replica
restart prime replica under partition