object as param value: not have these for now

add tests:
    how thorough?
        test each command?
        test all inputs?
        test styling?
    waiting until commands loaded how?
    test input how?
    test copy how?


remove reusing objects

stop propagation for refrence link

test commands: pay attention to filters

commands without known values we can supply (connect to cluster)
accessbility - required's color 
two tabs: read and write tabs /commonly used vs rarely used
safety rating: other ways to do it (group them into seperate groups ; comment out dangerous commands)