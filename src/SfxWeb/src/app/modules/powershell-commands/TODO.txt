add tests:
    how thorough?
        test each command?
        test all inputs?
        test styling?
    waiting until commands loaded how?
    test input how?
    test copy how?

test commands: pay attention to filters 

seperate parameters; optional parameter collapsible
tab: conditional render - what happens when only unsafe /dangerous avaliable
