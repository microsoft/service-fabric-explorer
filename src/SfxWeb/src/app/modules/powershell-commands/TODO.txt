add tests:
    how thorough?
        test each command?
        test all inputs?
        test styling?
    waiting until commands loaded how?
    test input how?
    test copy how?

test commands: pay attention to filters 

add required text + seperate parameters; optional parameter collapsible

tab: conditional render text; bigger font
switch over to reactive form controll