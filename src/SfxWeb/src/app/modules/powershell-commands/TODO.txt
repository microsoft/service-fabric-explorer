add tests:
    how thorough?
        test each command?
        test all inputs?
        test styling?
    waiting until commands loaded how?
    test input how?
    test copy how?

test commands: pay attention to filters 

add required text + seperate parameters; optional parameter collapsible

force remove on idle secondary

used spread operator on setupCommands
modal text, title
added variable to setting - stay on refresh; unsafe & dangerous linked