import { FormatParamsPipe } from './format-params.pipe';

describe('FormatParamsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatParamsPipe();
    expect(pipe).toBeTruthy();
  });
});
